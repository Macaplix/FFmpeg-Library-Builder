# speex pkg-config file

prefix=$PKG_CONFIG_PREFIX
exec_prefix=${prefix}
libdir=${exec_prefix}/libs
includedir=${exec_prefix}/include/speex

Name: speex
Description: speex is a library for ...
Version: 1.3.5
Requires:
Conflicts:
Libs: -L${libdir} -lspeex
Cflags: -I${includedir} -L${libdir}
